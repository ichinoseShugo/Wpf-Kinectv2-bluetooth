<Window x:Class="Wpf_Kinectv2_bluetooth.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf_Kinectv2_bluetooth"
        mc:Ignorable="d"
        Title="MainWindow" Height="640" Width="960"
        Loaded="Window_Loaded" Closing="Window_Closing">
    <Grid>
        <Image x:Name="ImageColor" Width="960" Height="540" />
        <ToggleButton x:Name="RecordPoints" Click="RecordPoints_Click" Content="Start Record" HorizontalAlignment="Left" Margin="0" VerticalAlignment="Bottom"/>
        <ToggleButton x:Name="ConnectButton" Click="ConnectButton_Click" Content="Start Listening" HorizontalAlignment="Left" VerticalAlignment="Bottom" HorizontalContentAlignment="Center" Margin="92,0,0,0.4"/>
        <TextBlock x:Name="Check" TextWrapping="Wrap" Text="Connect" Margin="250,0,-250.4,3.4" VerticalAlignment="Bottom"/>
        <Canvas x:Name="CanvasBody"  Width="512" Height="424"/>
        <StackPanel Orientation="Vertical">
            <TextBlock x:Name="TextBodyCount1"/>
            <TextBlock x:Name="TextBodyCount2"/>
            <TextBlock x:Name="TextBodyCount3"/>
        </StackPanel>
    </Grid>
</Window>
